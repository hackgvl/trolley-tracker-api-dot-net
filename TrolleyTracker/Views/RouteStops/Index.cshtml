@model IEnumerable<TrolleyTracker.Models.RouteStop>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StopSequence)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Route.ShortName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stop.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StopSequence)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Route.ShortName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stop.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
